# Copy this file to `.env` and fill in your credentials.
# Do not commit `.env` — it is excluded via .gitignore.

# A token with permission to query Dagster Cloud logs
DAGSTER_CLOUD_API_TOKEN=your-dagster-cloud-token

# OpenAI API key for LLM interactions
OPENAI_API_KEY=your-openai-api-key

# (Optional) Dagster Cloud GraphQL endpoint override
#DAGSTER_CLOUD_GRAPHQL_URL=https://dagster.cloud/api/graphql
